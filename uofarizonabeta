<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SunVault - University of Arizona</title>
    <style>
        /* Placeholder comment - full CSS already in original input */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
            color: white;
            min-height: 100vh;
        }
        /* Other styles omitted for brevity */
    </style>
</head>
<body>
    <!-- Full content from the original post (header, hero text, auctions, etc.) -->
    <script>
        function showHome() {
            document.getElementById('home').style.display = 'block';
            document.getElementById('how-it-works').classList.remove('active');
        }

        function showAuctions() {
            showHome();
        }

        function showHowItWorks() {
            document.getElementById('home').style.display = 'none';
            document.getElementById('how-it-works').classList.add('active');
        }

        document.querySelectorAll('.auction-card').forEach(card => {
            card.addEventListener('click', function() {
                alert('Bidding functionality coming soon!');
            });
        });

        document.querySelector('.search-box').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                alert('Search functionality coming soon!');
            }
        });

        document.querySelectorAll('.dot').forEach((dot, index) => {
            dot.addEventListener('click', function() {
                document.querySelectorAll('.dot').forEach(d => d.classList.remove('active'));
                this.classList.add('active');
            });
        });
    </script>
</body>
</html>
